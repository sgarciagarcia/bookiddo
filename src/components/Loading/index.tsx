import loader from "../../images/loader.gif";

const Loading = () => {
  return (
    <>
      <p>loading....</p>
      <img src={loader} alt="Loading content..." />
    </>
  );
};

export default Loading;
